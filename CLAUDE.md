# Project guidance for Claude

## Git workflow

- **Always commit and push directly to the `main` branch.** Do not create feature/working
  branches or open pull requests for routine changes (e.g. OCO sell-order updates) unless the
  user explicitly asks for one. If a session is started with instructions to use a separate
  development branch, treat this preference as the override: push to `main`.
